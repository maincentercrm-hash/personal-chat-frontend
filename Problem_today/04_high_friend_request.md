# 04 - HIGH PRIORITY: ปัญหา Friend Request System

**ลำดับความสำคัญ: 🔴 HIGH PRIORITY**
**ระดับความยาก: ⭐⭐ ปานกลาง**

---

## 📋 รายการปัญหา

### #15: ต้องรอให้เพื่อนยอมรับคำขอก่อนถึงทักแชทได้ (เราดูไม่ได้ว่าเคยเพิ่มเพื่อนใครไปแล้วบ้าง)

**ปัญหาแบ่งเป็น 2 ส่วน:**

---

## ส่วนที่ 1: ดูไม่ได้ว่าเคยส่ง Friend Request ให้ใครไปแล้วบ้าง

**ปัญหา:**
- หลังจากส่ง friend request ไปแล้ว ไม่มีที่แสดงรายชื่อคนที่เราส่งคำขอไป
- ไม่รู้ว่าคำขอยังค้างอยู่ หรือถูกปฏิเสธ หรือยอมรับแล้ว
- อาจส่งซ้ำ หรือไม่รู้ว่ากำลังรอ

**สาเหตุ:**
- ไม่มี UI แสดง "Sent Requests" หรือ "Pending Requests"
- ไม่มี API สำหรับดึงรายชื่อ pending requests

**วิธีแก้:**

### Frontend:
1. **สร้างหน้า Pending Friend Requests:**
   ```typescript
   interface FriendRequest {
     id: string;
     from: User;
     to: User;
     status: 'pending' | 'accepted' | 'rejected';
     createdAt: Date;
   }

   // UI Sections:
   // 1. Received Requests (คำขอที่เราได้รับ) - ต้อง Accept/Reject
   // 2. Sent Requests (คำขอที่เราส่งไป) - รอคนอื่น Accept
   ```

2. **Friend Request Status:**
   - แสดงสถานะใน User Profile:
     - "Add Friend" (ยังไม่เป็นเพื่อน)
     - "Request Sent" (เราส่งคำขอไปแล้ว)
     - "Accept Request" (เขาส่งคำขอมา)
     - "Friends" (เป็นเพื่อนแล้ว)

3. **Cancel Request:**
   - ให้สามารถยกเลิก pending request ที่เราส่งไป

**Backend ต้องทำ:**
✅ **ต้องทำ:**
1. **API Endpoints:**
   ```typescript
   GET /api/friend-requests/received    // คำขอที่เราได้รับ
   GET /api/friend-requests/sent        // คำขอที่เราส่งไป
   POST /api/friend-requests/{userId}   // ส่งคำขอ
   POST /api/friend-requests/{id}/accept
   POST /api/friend-requests/{id}/reject
   DELETE /api/friend-requests/{id}     // ยกเลิกคำขอที่เราส่ง
   ```

2. **Response Format:**
   ```json
   {
     "id": "req_123",
     "from": {
       "id": "user_456",
       "username": "john_doe",
       "displayName": "John Doe",
       "avatar": "..."
     },
     "to": {
       "id": "user_789",
       "username": "jane_doe",
       "displayName": "Jane Doe",
       "avatar": "..."
     },
     "status": "pending",
     "createdAt": "2024-01-01T10:00:00Z"
   }
   ```

3. **WebSocket Events:**
   - `friend_request.received` - เมื่อได้รับคำขอ
   - `friend_request.accepted` - เมื่อคำขอถูกยอมรับ
   - `friend_request.rejected` - เมื่อคำขอถูกปฏิเสธ
   - `friend_request.cancelled` - เมื่อผู้ส่งยกเลิก

4. **Prevent Duplicate Requests:**
   - ตรวจสอบว่ามี pending request อยู่แล้วหรือไม่
   - Return error ถ้าพยายามส่งซ้ำ

---

## ส่วนที่ 2: ต้องรอให้เพื่อนยอมรับคำขอก่อนถึงทักแชทได้

**ปัญหา:**
- ไม่สามารถทักแชทกับคนที่ยังไม่เป็นเพื่อนได้
- ทำให้ใช้งานไม่สะดวก

**ตัวเลือกในการแก้:**

### Option 1: เปิดให้ทักได้โดยไม่ต้องเป็นเพื่อนก่อน (แนะนำ)
**เหมือน Facebook Messenger, Instagram, Discord**

**Pros:**
- ใช้งานง่าย สะดวก
- ส่งข้อความได้ทันที
- เหมาะกับ business use case

**Cons:**
- อาจมี spam
- ต้องมีระบบ Message Requests

**Implementation:**
1. **Message Requests:**
   - ข้อความจากคนที่ไม่เป็นเพื่อน → ไปที่ "Message Requests"
   - ผู้รับต้อง Accept ก่อนถึงจะเห็นเป็น conversation ปกติ

2. **Privacy Settings:**
   - ตั้งค่าว่าใครส่งข้อความมาได้บ้าง:
     - Everyone
     - Friends of Friends
     - Friends Only

**Backend ต้องทำ:**
✅ **ต้องทำ:**
```typescript
// Conversation Types
enum ConversationType {
  NORMAL = 'normal',        // เพื่อนกัน
  MESSAGE_REQUEST = 'message_request'  // ยังไม่เป็นเพื่อน
}

POST /api/conversations/message-request/{userId}  // ส่งข้อความ + Friend Request
GET /api/conversations/message-requests           // รายการ Message Requests
POST /api/conversations/message-requests/{id}/accept  // Accept
POST /api/conversations/message-requests/{id}/reject  // Reject
```

---

### Option 2: ต้องเป็นเพื่อนก่อน (แบบเดิม แต่ปรับ UX)
**เหมือน LINE, WhatsApp**

**Pros:**
- Privacy สูง
- ไม่มี spam

**Cons:**
- ใช้งานไม่สะดวก
- ต้องรอให้ Accept Friend Request ก่อน

**Improvement:**
1. แสดงสถานะชัดเจนว่า "รอยอมรับเป็นเพื่อน"
2. ทำให้ Friend Request process ง่ายขึ้น
3. แสดง pending requests เด่นๆ

---

### Option 3: Hybrid (แนะนำที่สุด)
**เหมือน Telegram**

- ส่งข้อความได้ แต่ต้องผ่าน Message Request
- Accept Message Request = Accept Friend Request
- ได้ทั้งความสะดวกและ privacy

**Implementation:**
```typescript
// User Flow:
// 1. A ส่งข้อความหา B (ไม่เป็นเพื่อนกัน)
// 2. B เห็นใน "Message Requests"
// 3. B Accept → กลายเป็น conversation ปกติ + เป็นเพื่อนกัน
// 4. B Reject → A ไม่สามารถส่งข้อความอีก (หรือจำกัดได้)
```

---

## 🎯 แผนการแก้ไข (เรียงตามลำดับ)

### Phase 1: Analysis & Decision (30 นาที - ต้องตัดสินใจก่อน)
1. เลือก Option (1, 2, หรือ 3)
2. วาง User Flow
3. ประสานกับ Backend

### Phase 2: Backend Development (ทีม Backend ทำก่อน)
1. Friend Request APIs
2. Message Request APIs (ถ้าเลือก Option 1 หรือ 3)
3. WebSocket Events
4. Database Schema

### Phase 3: Frontend - Friend Requests UI (2-3 ชม.)
1. สร้างหน้า Friend Requests
   - Tab: Received
   - Tab: Sent
2. Add Friend button states
3. Accept/Reject actions
4. Real-time updates

### Phase 4: Frontend - Message Requests UI (2-3 ชม.) - ถ้าใช้ Option 1/3
1. สร้างหน้า Message Requests
2. Accept/Reject message requests
3. Integration กับ conversation list

### Phase 5: Testing
1. Test friend request flow
2. Test message request flow (ถ้ามี)
3. Test notifications
4. Test real-time updates

---

## 📦 ไฟล์ที่ต้องแก้

**Frontend:**
- `src/pages/FriendRequests.tsx` (สร้างใหม่)
- `src/pages/MessageRequests.tsx` (สร้างใหม่ - ถ้าใช้ Option 1/3)
- `src/components/Friends/FriendRequestItem.tsx`
- `src/components/Profile/AddFriendButton.tsx`
- `src/services/api/friends.ts`
- `src/stores/friendStore.ts`
- `src/services/websocket.ts` - Event handlers

**Backend (ต้องทำ):**
- Friend Request APIs
- Message Request APIs
- WebSocket Events
- Database migrations

---

## 🎨 UI/UX Design Suggestions

### Friend Requests Page:
```
┌─────────────────────────────────┐
│  Friend Requests                │
├─────────────────────────────────┤
│  [Received] [Sent]              │
├─────────────────────────────────┤
│  📬 Received (3)                │
│  ┌───────────────────────────┐ │
│  │ 👤 John Doe               │ │
│  │    Sent 2 hours ago       │ │
│  │    [Accept] [Reject]      │ │
│  └───────────────────────────┘ │
│                                 │
│  📤 Sent (2)                    │
│  ┌───────────────────────────┐ │
│  │ 👤 Jane Smith             │ │
│  │    Sent 1 day ago         │ │
│  │    [Cancel Request]       │ │
│  └───────────────────────────┘ │
└─────────────────────────────────┘
```

### Add Friend Button States:
```typescript
// Not friends
<Button>Add Friend</Button>

// Request sent
<Button disabled>Request Sent ✓</Button>

// Request received
<Button variant="primary">Accept Request</Button>

// Already friends
<Button disabled>Friends ✓</Button>

// Blocked
<Button disabled>Blocked</Button>
```

---

## ✅ เงื่อนไขการ Test

**Friend Requests:**
- [x] ส่ง friend request → เห็นใน "Sent Requests"
- [x] ได้รับ friend request → เห็นใน "Received Requests"
- [x] Accept request → เป็นเพื่อนกัน, หายจาก requests
- [x] Reject request → ไม่เป็นเพื่อน, หายจาก requests
- [x] Cancel request → หายจาก "Sent Requests"
- [x] Real-time update (WebSocket)
- [x] Notification เมื่อได้รับ/ยอมรับคำขอ

**Message Requests (ถ้ามี):**
- [ ] A ส่งข้อความหา B (ไม่เป็นเพื่อน) → B เห็นใน Message Requests
- [ ] B Accept → กลายเป็น conversation ปกติ + เป็นเพื่อน
- [ ] B Reject → conversation หาย, A ไม่สามารถส่งข้อความอีก
- [ ] Privacy settings ทำงาน

---

## 💡 คำแนะนำ

**ผมแนะนำให้ใช้ Option 3 (Hybrid)** เพราะ:
1. ใช้งานสะดวก - ส่งข้อความได้เลย
2. Privacy ดี - ต้อง Accept ก่อน
3. UX ดี - ไม่ต้องทำ 2 action (Add Friend แล้วค่อยแชท)
4. ทันสมัย - เหมือน Telegram, LinkedIn

แต่ต้องประสานกับ Backend ให้ดีครับ
